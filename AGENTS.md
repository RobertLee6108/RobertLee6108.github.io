# Agent Instructions

- When modifying this Jekyll blog, always ensure `jekyll build` runs successfully.
- Posts should go into `_posts` with `YYYY-MM-DD-title.md` format.
- Do not edit files in `_site` directory as they are generated automatically.
